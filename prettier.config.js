/** @type{import('prettier').Config} */
const config = {
  singleQuote: true,
  semi: false,
  arrowParens: 'always',
  bracketSpacing: true,
  printWidth: 90,
  tabWidth: 2,
  trailingComma: 'none'
}

export default config
